#include <stdio.h>

/**
 *main-Entry point
 *
 *return-Always 0 (Success)
 */
 
int main(void)
{
	
	/**
	*char-define characters
	*char(str)[]="Programming is like building a multilingual puzzle\n";
	*
	*puts-insert characters
	*puts(str);
	*
	*return-description of the return value
	*return (0);
	*/
}
