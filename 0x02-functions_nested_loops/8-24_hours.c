#include "main.h"

/**
 * jack_bauer - prints every minute of the day
 *
 * Return: Always 0 (success)
 */

void jack_bauer(void)
{
	int a, b, c, d;

	for (a = 0; a <= 2; a++)
	{
	for (b = 0; b <= 4; b++)
	{
		if (a >= 2 && b >= 40
		{
			break;
		}
	for (c = 0; c <= 5; c++)
	{
	for (d = 0; d <= 9; d++)
	{
		{
		_putchar(a + 48);
		_putchar(b + 48);
		_putchar(58);
		_putchar(c + 48);
		_putchar(d + 48);
		_putchar('\n');
		}
	}
	}
	}
	}
}
